# 微金100 前端开发 · 问题汇总（脚手架阶段 v0.1）

> 本文件汇总前端脚手架与首批页面开发过程中的**取舍、假设、待确认项**，按受众分类。需后端/业务/设计拍板的，分别指回《后端接口对接_待确认清单.md》《微金100_业务规则确认表.html》。
> 更新于 2026-06-24。

---

## 一、本期已完成范围（`frontend/`，原生小程序 + TS，全程 Mock 跑通）

- **工程**：脚手架 + 分包（packageLock / packageMine）+ tabBar；`USE_MOCK=true` 离线可跑。
- **基础设施**：设计令牌、请求层（信封解包 / 错误码 / 401 刷新 / Mock 开关）、鉴权（`wx.login`→JWT、`getPhoneNumber`）、静默校验守卫、格式化、类型、Mock、API。
- **组件库（8 个）**：price-card（金价卡）、goods-row（挂单行）、lv-badge（等级徽标）、tag（标签）、result（结果页）、sheet（底部弹窗）、number-keyboard（数字键盘）、uploader（图片上传）。
- **页面**：首页 home（行情大厅）、行情 market（金属切换）、我的 mine、订单 order（三板块）、发布 publish（入口/须知）、买家锁价主页 lock；守卫补全页 kyc / profile-edit / margin-recharge 为占位。
- **接口规范**：`api-spec/openapi.yaml`（OpenAPI 3.0 草案，20 路径，可导入 Apifox）。
- **校验**：结构 + import 解析全通过（9 页 / 组件 / 引用）；OpenAPI 无悬空 $ref。

---

## 二、已知限制 / 本期未做（取舍，后续迭代）

| # | 项 | 说明 |
|---|---|---|
| 1 | **完整发布表单** | 仅做「发布须知 + 入口」页；金属/性质/数量/出货方式/定价两模式/图片上传/确认等表单**未做**。`packagePublish` 分包未建（代码内 TODO 标注）。 |
| 2 | **买家锁价仅主页** | 数量 + 双价试算 + 确认锁价已通；**确认弹窗 / 处理中缓冲 / 成功(双价/单价) / 失败 / 联系卖家** 屏未做（提交后 toast 占位）。 |
| 3 | **我的子页占位** | 实名认证 / 个人资料 / 保证金充值 / 级别佣金 / 违约 / 地址 / 反馈 / 客服 均为 stub。 |
| 4 | **订单深页未做** | 订单详情 / 确认完成 / 仲裁 / 平台代交接 未做（列表点卡 toast「待开发」）。 |
| 5 | **实时/支付/实名** | WebSocket 行情、微信支付、实名 OCR、订阅消息推送 均未接（行情用 REST Mock）。 |
| 6 | **守卫拦截未生效** | 补全页占位 + Mock 资质全通过，故「锁价/发布」当前直接放行；真实拦截待补全页接入。 |
| 7 | **未做真编译/真机** | 仅结构 + import 解析校验。建议 `cd frontend && npm install && npm run typecheck`，并在微信开发者工具中实测渲染（可能有个别 TS/像素微调）。 |

---

## 三、需后端确认（开发中发现，详见《后端接口对接_待确认清单》）

1. **金额单位**：前端按「**分**」整数处理（`fenToYuan`）。需后端确认（清单 §5）。
2. **等级格式**：前端按 `'L<n>'`（如 `L7`）解析做分层变色。需确认字段格式。
3. **金属切换**：`/market/quote`、`/market/listings` 是否支持 `metal=silver|platinum`（否则白银/铂金 tab 空列表，前端已加空态兜底）。
4. **响应结构**：列表统一 `Paged<T>`（`data.list`）+ 信封 `{code,bizCode,message,data}`，需确认（清单 §2）。
5. **订单角标**：前端用 `/orders/badge`（`{pendingCount}`）；若要求仅用 `/orders` 前端分组，需调整。两接口口径须一致。
6. **锁价机制**：价格快照 `snapshotVersion` + 有效期、提交幂等键、处理中轮询/推送（清单 §3/§4）。
7. **图片上传**：本期 uploader 仅本地预览；真接口需 `wx.uploadFile`→fileId（直传 STS 还是经后端，清单 §7）。

---

## 四、需业务 / 产品确认（详见《业务规则确认表》）

1. **锁价主页文案**：「4 小时履约」等以确认表 **A1/A2** 为准。
2. **数量规则**：购买上限公式（C6）、散出起批量（F4）、整出固量份重（F3）、发布溢价范围（F5）等。
3. **订单状态配色**：`tag` 组件目前只有 cat/zheng/san/spot 四类视觉色，订单状态（销售中/锁价待处理/已完成/仲裁中/代交接…）用了**近似映射**，非语义专属色。是否需为订单状态定义独立配色？

---

## 五、需设计确认

1. **保证金卡渐变色**：令牌无现成「奶油金渐变」，`mine` 页临时用 `linear-gradient(160deg,#FDF4E1,#F6E7C6)`，需设计给准色值（或补一个令牌）。
2. **额度千分位**：`mine` 页三金属额度（如 `6000g`）当前未加千分位，是否需要 `6,000g`。
3. **price-card 文案**：已改为按 `metal` 动态显示「实时金价/银价/铂价」（修复了硬编码「金价」）。

---

## 六、技术约定与坑（给后续开发 / 其他 Claude）

- **WXML 不能调用 import 的函数** → 所有展示数据在 `.ts` 用「**VM 模式**」预格式化（`fenToYuan`、`quoteTimeText`、`avatarChar`、`moqText`、倒计时文案等），WXML 纯展示。各页已遵循此范式，新增页请照做。
- **设计令牌（CSS 自定义属性）** 定义在 `page` 上，会**继承进组件**，组件内可直接 `var(--xxx)`。
- **Mock 开关** 在 `config/env.ts` 的 `USE_MOCK`；`request.ts` 据此走 `mock/` 或真实 `wx.request`。联调改 `false` 并配 `BASE_URL`。
- **新增页面/分包** 必须在 `app.json` 注册（tab 页进 `pages`，分包进 `subPackages`）。
- **金额/合计/限额** 一律以后端为准，前端只展示与试算。
- 组件事件用 `triggerEvent` + 父 `bind:xxx`；`this.data` 可读 properties。
