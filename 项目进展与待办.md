# 微金100 小程序 · 项目进展与待办（Project Status）

> 更新日期：2026-06-30（R20）　|　阶段：前后端开发完成 → 联调 / 测试 / 上线准备
> 产品：微信小程序，**前后端分离**，贵金属（黄金/白银/铂金）C2C 撮合 + 线下交割平台。

---

## 一、当前进展（Done）

### 1. UI 设计稿 ✅ 全部完成（5 模块 / 约 45 屏）

| 模块 | 屏数 | 状态 |
|---|---|---|
| 首页 / 行情大厅 | 4 | ✅ 含金价卡、筛选、列表、订阅提醒 |
| 买家锁价 | 8 | ✅ 整出全量/固量、散出、确认、成功/失败 |
| 我的 / 个人中心 | ~14 | ✅ 资料、实名、保证金、充退款、级别、违约、客服 |
| 我的订单 | ~10 | ✅ 三 Tab、详情、确认完成、仲裁、代交接 |
| 货品发布 | ~9 | ✅ 表单、出货要求、定价两模式、图片、确认 |

### 2. 前端 ✅ 全部页面实现（23 页，0 占位）

- 5 主 Tab + 锁价全流程 + 发布全流程 + 订单详情/仲裁/代交接
- 9 个 packageMine 子页（资料/实名/保证金/充值/级别/违约/申诉/地址/提醒）
- 反馈页、关于页；所有菜单路由全部接通；筛选 chips 接 API；地图导航；客服微信
- **本轮新增（R20）**：首页 WebSocket 行情订阅（`market.socket.ts`），实盘时自动切换为实时推送，Mock 模式保持 REST；断线自动重连（指数退避）

### 3. 后端 ✅ Sprint 0–6a 实现（真库，42 接口）

| Sprint | 内容 | 状态 |
|---|---|---|
| 0 | 基建(Prisma/NestJS/JWT/infra) | ✅ |
| 1 | 微信登录/我的/保证金/级别 | ✅ |
| 2 | 挂单/地址/金价提醒 | ✅ |
| 3 | 锁价核心闭环（快照/原子扣库/冻结/双确认/解冻） | ✅ |
| 4 | 违约/申诉/违约判定 | ✅ |
| 5 | 文件上传/下载、定时任务、挂单筛选、代交接步骤更新 | ✅ |
| 6a | **金价提醒触发、锁价幂等、健康检查 DB、Prisma 迁移** | ✅ 本轮 |

**本轮新增（Sprint 6a / R20）：**
- `MarketService.checkAlerts()`：每次行情轮询后自动检测触发条件，命中即禁用提醒；Sprint6 接入微信订阅消息后一行代码接入推送
- `LockService` 内存幂等锁：防同一用户并发重复提交同一挂单（Redis 就绪后替换为分布式锁）
- `GET /health` 返回 `db: 'ok'|'error'` 数据库连通性字段
- `prisma/migrations/20260630000000_add_arbitration_and_address_coords`：补齐 `Order.arbitratingStartAt`、`Address.latitude/longitude` 字段迁移文件
- `.env.example` 补充微信订阅消息模板 ID 占位变量

---

## 二、剩余待办（上线前必须完成）

### 🔴 P0 — 合规/资质（硬阻塞，商务/法务负责）

- [ ] 微信小程序贵金属/金融类目审核资质
- [ ] ICP 备案（域名 + 服务器）
- [ ] 微信支付商户号（充值/代交接¥100 真扣款）
- [ ] 微信实名核身 API 资质（OCR + 人脸）
- [ ] 注册真实小程序 AppID（当前用 dev 测试号）
- [ ] 法务文案：用户协议 / 隐私政策 / 交易规则 / 违约细则

### 🔴 P0 — 外部资源

- [ ] 行情 IP 授权：向脉动 PulseData 申请 ECS 公网 IP 白名单（正式端口 1008）
- [ ] 铂金行情代码（RT_PT 未授权）
- [ ] 生产服务器（ECS）+ 生产 PostgreSQL + 生产 Redis

### 🟠 P1 — 外部依赖对接（需资质/账号后对接）

- [ ] 微信支付 V3 真实对接（替换 mock，充值/代交接¥100）
- [ ] 实名 OCR + 人脸核身 API（替换 dev 直通 `verified`）
- [ ] 微信订阅消息模板 ID 填入 `.env` + 推送逻辑（`checkAlerts` 中预留 TODO Sprint6）
- [ ] Redis 生产实例（BullMQ 定时任务升级；锁价幂等替换内存 Set → Redis SET NX）
- [ ] 短信网关接入（可选，订阅消息优先）

### 🔵 P2 — 联调 & 测试

- [ ] 前后端 Mock → 真接口逐模块联调（USE_MOCK=false）
- [ ] 微信支付全链路沙盒测试 → 真实测试
- [x] WebSocket 行情推送（前端已实现，后端 WS 就绪，等 USE_MOCK=false 联调）
- [ ] 弱网/断网/重连场景测试
- [ ] 多机型真机测试（iPhone SE / 14 Pro / Android 主流机型）
- [ ] 金额越权安全测试

### 🔵 P3 — 部署 & 上线

- [ ] `docker-compose.prod.yml` + Nginx 本机验证（文件已就绪，待 ECS）
- [ ] 域名解析 + SSL 证书（Nginx 配置中 HTTPS 段已预留注释）
- [ ] 数据库定时备份（`deploy/backup.sh` 接 cron `0 3 * * *`）
- [ ] 监控告警：`GET /api/v1/health` 已返回 `db` + 行情健康；接入 UptimeRobot / 阿里云告警
- [ ] 埋点：浏览 → 锁价 → 完成漏斗
- [ ] 微信小程序提审材料（类目资质截图 + 功能说明 + 测试账号）
- [ ] 灰度发布计划（内测 → 体验 → 正式）

---

## 三、里程碑状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| Sprint 0 | 选型 + 基建 | ✅ |
| Sprint 1–4 | 核心业务模块 | ✅ |
| Sprint 5 | 文件/任务/筛选补全 | ✅ |
| Sprint 6a | 行情触发/幂等/健康检查 | ✅ 本轮完成 |
| Sprint 6b | 外部依赖对接（支付/OCR/推送）| ⏳ 待资质 |
| Sprint 7 | 联调 + 测试 | ⏳ |
| Sprint 8 | 提审 + 灰度 + 上线 | ⏳ |

> 当前阻塞点：**合规资质**（最长路径，建议立即启动）。代码层面已可进入联调。

---

## 四、仓库文件清单

| 文件/目录 | 说明 |
|---|---|
| `README.md` | 仓库导航 |
| `HANDOFF.md` | 项目交接 + 逐轮工作记录 |
| `项目进展与待办.md` | 本文件 |
| `frontend/` | 原生小程序（TS，23 页，0 占位） |
| `backend/` | NestJS（42 接口，真库） |
| `mock-app/` | H5 单机演示版（无需微信工具） |
| `*_设计稿.html` | UI 设计稿（5 份） |
| `微金100.pdf` | 设计规范 |
| `业务规则确认结果.md` | v1.0 定稿 |
| `api-spec/openapi.yaml` | OpenAPI 草案 |
