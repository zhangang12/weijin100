# 微金100 Nginx（单 ECS）：HTTPS 反代 + WebSocket + 商品图静态
# 证件等敏感文件不在此暴露，走后端鉴权下载接口。

upstream weijin_api {
    server backend:3100;
}

# HTTP：有证书后改为 301 跳 HTTPS
server {
    listen 80;
    server_name _;

    location /api/ {
        proxy_pass http://weijin_api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ws/ {
        proxy_pass http://weijin_api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
    }

    # 仅商品图（公开）；证件走 /api 鉴权下载
    location /uploads/ {
        alias /data/uploads/;
        expires 7d;
    }
}

# HTTPS（配好证书后启用，并把上面的 80 改为跳转）：
# server {
#     listen 443 ssl http2;
#     server_name your.domain.com;
#     ssl_certificate     /etc/letsencrypt/live/your.domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your.domain.com/privkey.pem;
#     location /api/ { proxy_pass http://weijin_api; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto https; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; }
#     location /ws/  { proxy_pass http://weijin_api; proxy_http_version 1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade"; proxy_read_timeout 3600s; }
#     location /uploads/ { alias /data/uploads/; expires 7d; }
# }
