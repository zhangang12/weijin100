# 微金100 · 资源准备清单（开工 Sprint 1 前备齐）

> 决策：先备齐关键资源再开工。本清单按负责人分组，可分段转发。
> 标 **⛔** = 硬阻塞（不给就卡对应环节）。最小启动集见末尾。
> 配套：[技术方案与开发计划_v1.md](技术方案与开发计划_v1.md)（路线图/架构）。

## A. 业务 / 法务（周期最长，最先启动）

| # | 项目 | 怎么拿 / 交付物 | 解锁 | 周期 |
|---|---|---|---|---|
| A1 | ⛔ 小程序主体 + **「贵金属/金融」类目资质** | 微信公众平台提交主体认证 + 选对应服务类目，按要求上传资质 | 上线/提审 | 长 |
| A2 | ⛔ **ICP 备案 + 小程序备案 + HTTPS 域名** | 阿里云买域名 → ICP 备案 → 微信侧小程序备案；域名配 HTTPS | 上线 | 长 |
| A3 | ⛔ **微信支付商户资质 + 商户号** | 微信支付商户平台申请，需营业执照/对公账户 | 保证金/支付 | 中长 |
| A4 | 法务文案：用户协议 / 隐私政策 / 交易规则 / **买卖双方违约细则** / 发布合规 5 条 | 法务定稿文本（给我 → 嵌入小程序与后端校验提示） | 提审/合规 | 中 |
| A5 | 业务给值：**正式佣金费率表**、保证金/限额最终值 | 运营期免佣，正式费率后期给；**当前按占位可配置，不阻塞** | 不阻塞 | 中 |

## B. 微信（解锁登录与支付）

| # | 项目 | 怎么拿 / 交付物 | 解锁 |
|---|---|---|---|
| B1 | ⛔ **小程序 AppID + AppSecret** | 微信公众平台 → 开发 → 开发管理 → 开发设置；复制 **AppID** 与 **AppSecret** 给我（写入后端 `.env` 的 `WX_APPID/WX_SECRET`） | **Sprint 1 登录** |
| B2 | 微信支付：商户号 **MCHID** + **APIv3 密钥** + 商户**证书序列号/私钥** | 微信支付商户平台下载/设置 | 保证金充值、代交接¥100 |
| B3 | 订阅消息模板 ID（金价提醒 / 订单通知） | 公众平台 → 订阅消息 → 申请模板 | 通知/提醒 |

## C. 行情数据源（脉动 PulseData）

| # | 项目 | 怎么拿 / 交付物 | 解锁 |
|---|---|---|---|
| C1 | ⛔ 采购正式账号 + **把后端服务器公网 IP 发客服 `@mdapi888` 授权**（切 1008 端口） | 否则正式端口返回「IP 未授权」 | 行情正式上线 |
| C2 | **铂金正式订阅代码** | 问数据商（免费库 `RT_PT` 显示「未授权」，铂金需正式库） | 铂金行情 |
| C3 | 确认融通金单位/口径 | RT_AU 当前测试值 891，确认是「元/克」及销售/回购/溢价业务口径 | 价格口径 |

## D. 阿里云 / 运维（解锁联调与部署）

| # | 项目 | 怎么拿 / 交付物 | 解锁 |
|---|---|---|---|
| D1 | ⛔ **RDS PostgreSQL** 实例 | 开通后给我连接串 → 填 `DATABASE_URL`（本地开发也可先用 docker） | **Sprint 1 数据落库** |
| D2 | ⛔ **Redis** 实例 | 给我连接串 → 填 `REDIS_URL`（本地可先 docker） | **Sprint 1 会话/锁/队列** |
| D3 | **ECS 服务器**（部署后端） | 其**公网 IP** 即用于 C1 授权 | 部署/联调 |
| D4 | **OSS** Bucket + AK/SK | 证件/商品图直传 | 实名/发布上传 |
| D5 | 短信签名 + 模板 + AccessKey | 阿里云短信控制台 | 金价提醒/通知 |
| D6 | 实名核身（OCR + 人脸）能力开通 | 需资质，阿里云/三方 | 实名认证 |

---

## 🚀 最小启动集（给齐这 3 样我就开 Sprint 1）

> **B1（AppID + AppSecret）** + **D1（PostgreSQL）** + **D2（Redis）**

给齐后我即：接 Prisma（建表 + 种子数据）→ 微信登录 `jscode2session` + JWT + 游客态 → 我的 / 实名 / 保证金 / 级别 走真库。
> 没拿到 AppID 也能先做：登录用**可插拔 mock 微信客户端**，凭证到位即切真——若你希望我**先用 mock 起步**，告诉我一声即可。

## ✅ 已就绪 / 无需你操心
- 行情真链路（测试源 RT_AU）已通；NestJS 骨架 + 16 表数据模型 + 本地 docker(pg/redis) 已就位。
- 销售/回购/溢价、佣金 = **占位可配置**（`PRICE_CFG` / 运营期免佣），业务给正式值后改配置即可。
