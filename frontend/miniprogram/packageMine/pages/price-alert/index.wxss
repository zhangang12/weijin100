.page { height: 100vh; box-sizing: border-box; padding: 20rpx 24rpx 0; }

/* 顶部说明 */
.note {
  font-size: 22rpx; color: var(--muted); line-height: 1.6;
  background: var(--cream); border: 1rpx solid rgba(184, 145, 63, .22);
  border-radius: 14rpx; padding: 16rpx 20rpx; margin-bottom: 20rpx;
}

/* 空 / 加载态 */
.empty { padding: 120rpx 0; text-align: center; color: var(--muted); font-size: 26rpx; }
.empty-ic { font-size: 72rpx; margin-bottom: 16rpx; }
.empty-t { font-size: 26rpx; color: var(--muted); }

/* 提醒列表 */
.list { display: flex; flex-direction: column; gap: 20rpx; }
.alert {
  background: var(--card); border-radius: var(--radius);
  padding: 24rpx; box-shadow: var(--shadow);
  display: flex; align-items: center; justify-content: space-between; gap: 20rpx;
}
.alert-main { flex: 1; min-width: 0; }
.alert-line { display: flex; align-items: baseline; gap: 12rpx; flex-wrap: wrap; }
.metal { font-size: 30rpx; font-weight: 800; color: var(--ink); }
.cond { font-size: 26rpx; font-weight: 700; }
.cond.up { color: var(--up); }
.cond.down { color: var(--down); }
.price { font-size: 32rpx; font-weight: 800; color: var(--ink); font-variant-numeric: tabular-nums; }
.unit { font-size: 22rpx; color: var(--muted); }

.chans { display: flex; gap: 10rpx; margin-top: 14rpx; }

/* 删除 */
.del {
  font-size: 24rpx; font-weight: 700; color: var(--up);
  padding: 8rpx 24rpx; border-radius: 999rpx;
  border: 1rpx solid rgba(214, 69, 65, .25); background: #FFEBE7;
  flex-shrink: 0;
}

.bottom-space { height: 160rpx; }

/* 底部按钮 */
.cta {
  position: fixed; left: 0; right: 0; bottom: 0;
  padding: 16rpx 24rpx calc(16rpx + env(safe-area-inset-bottom));
  background: linear-gradient(180deg, rgba(242, 239, 234, 0), var(--paper) 40%);
}
.btn {
  height: 92rpx; border-radius: 20rpx;
  background: linear-gradient(180deg, var(--btn-1), var(--btn-2)); color: #fff;
  font-size: 32rpx; font-weight: 800;
  display: flex; align-items: center; justify-content: center;
  box-shadow: 0 12rpx 28rpx rgba(184, 145, 63, .32);
}

/* 表单（弹窗内） */
.form { padding: 8rpx 0 16rpx; }
.field {
  display: flex; align-items: center; gap: 20rpx;
  padding: 22rpx 0; border-bottom: 2rpx solid var(--line);
}
.field-col { flex-direction: column; align-items: stretch; gap: 18rpx; }
.field-k { font-size: 28rpx; font-weight: 700; color: var(--ink-2); min-width: 132rpx; }
.ipt { flex: 1; height: 56rpx; font-size: 28rpx; color: var(--ink); text-align: right; }
.ph { color: var(--faint); }

/* segment */
.seg { flex: 1; display: flex; background: var(--paper); border-radius: 999rpx; padding: 6rpx; }
.seg-i {
  flex: 1; height: 60rpx; line-height: 60rpx; text-align: center;
  font-size: 26rpx; color: var(--ink-2); border-radius: 999rpx; transition: all .18s;
}
.seg-i.on {
  background: linear-gradient(180deg, var(--btn-1), var(--btn-2));
  color: #fff; font-weight: 700; box-shadow: 0 6rpx 16rpx rgba(184, 145, 63, .3);
}

/* 目标价输入 */
.price-box { flex: 1; display: flex; align-items: center; justify-content: flex-end; gap: 10rpx; }
.prefix { font-size: 30rpx; font-weight: 800; color: var(--gold-3); }
.price-ipt { flex: 1; text-align: right; font-weight: 800; font-variant-numeric: tabular-nums; }
.suffix { font-size: 24rpx; color: var(--muted); }

/* 提醒方式多选 */
.chk-row { display: flex; gap: 16rpx; }
.chk {
  flex: 1; height: 76rpx; line-height: 76rpx; text-align: center;
  font-size: 26rpx; font-weight: 700; color: var(--ink-2);
  background: var(--paper); border: 1rpx solid var(--line); border-radius: 16rpx;
  transition: all .18s;
}
.chk.on {
  color: var(--gold-3); background: var(--cream);
  border-color: rgba(184, 145, 63, .45);
}

/* 开启提醒按钮 */
.save-btn {
  margin-top: 32rpx; height: 92rpx; border-radius: 20rpx;
  background: linear-gradient(180deg, var(--btn-1), var(--btn-2)); color: #fff;
  font-size: 32rpx; font-weight: 800;
  display: flex; align-items: center; justify-content: center;
  box-shadow: 0 12rpx 28rpx rgba(184, 145, 63, .32);
}
.save-btn.disabled { opacity: .6; }
